// import create from 'zustand';
// import { MyStateType } from './type';

// interface StoreState {
//   user: MyStateType;
//   setUser: (user: MyStateType) => void;
// }

// export const useStore = create<StoreState>((set) => ({
//   user: { id: '', name: '' }, // 초기 상태 설정
//   setUser: (user) => set({ user }),
// }));
